// put here composable to share data or functionality
// example:
/*
  export default function useBaseComposable(): IBaseComposable {
    const counter: Ref<number> = ref<number>(0);
    const increase = (): number => counter.value ++;

    return {
      counter,
      increase
    }
  }
*/